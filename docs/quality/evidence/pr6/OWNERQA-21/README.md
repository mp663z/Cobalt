# Closeout audit

The proposed delivery is one companion-CLI PR against `beta`, alongside the already separate app-polish PR. That is two logically grouped PRs total, below the maximum of three. The integrator must apply merged-lane patches, refresh the test counts against the resulting head, show the title/body to the user for review, then open the PR. This file does not authorize or perform the public post.
