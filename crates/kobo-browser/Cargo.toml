[package]
name = "kobo-browser-core"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish = false
description = "Navigation state for the e-ink reader browser: history, fragments, positions."

[dependencies]
kobo-web-document = { path = "../kobo-web-document" }

[lints]
workspace = true
